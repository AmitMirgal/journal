{
    "name": "contract",
    "version": "1.0.0",
    "scripts": {
        "build": "npx ncc build src/mycontract.js -o dist",
        "dev": "npm run build && hpdevkit deploy dist"
    },
    "dependencies": {
        "hotpocket-nodejs-contract": "0.5.6",
        "@vercel/ncc": "0.34.0"
    }
}